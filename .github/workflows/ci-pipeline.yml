name: devsecops-pipeline

on:
  push:
    branches:
      - main

jobs:
  sast_sonar:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Placeholder Sonar Scan
        run: |
          echo "SAST step placeholder. SonarQube scan will run here."
          echo "Esto evita que el pipeline esté vacío."
